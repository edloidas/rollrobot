const should = require( 'should' );

// Just a dummy test for now
describe( 'server', () => {
  let msg = 'should start without errors';
	it( msg, ( done ) => {
    // should expression here
		done();
	});
});
